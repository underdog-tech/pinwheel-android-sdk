version: 2.1

jobs:
  build_and_prepare:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: Done
          command: echo Done

workflows:
  master-workflow:
    jobs:
      - build_and_prepare:
          context: 'frontend-production'
